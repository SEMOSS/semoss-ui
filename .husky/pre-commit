#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

npx lint-staged && npx vitest --watch=false --passWithNoTests --changed || {
  echo "ERROR: Linting or testing issues were found in the committed files. Please address them before proceeding."
  exit 1
}

