// Public surface of the notebook editor mirrors what the single-file version
// exposed: only the top-level component. The subcomponents and utilities are
// internal and imported directly by siblings within this folder.
export { Notebook } from "./notebook";
