# Development

## Common errors

Unable to find `@semoss/sdk`, the sdk library hasn't been built for local development

Change directories to `libs/sdk`, and run the build command ``

Unable to find `@semoss/ui`, the component library hasn't been built for local development

Change directories to `libs/ui`, and run the build command ``
