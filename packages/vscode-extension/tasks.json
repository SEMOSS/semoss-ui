{
	"version": "1.0.0",
	"tasks": [
		{
			"label": "Link Semoss Instance",
			"command": "${input:variableID}"
		}
	],
	"inputs": [
		{
			"id": "variableID",
			"type": "type of input variable"
		}
	]
}
