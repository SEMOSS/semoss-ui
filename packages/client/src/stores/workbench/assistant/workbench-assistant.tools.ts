// The isRequestUserInputAction check is shared platform-wide (workbench,
// playground, etc.) via @semoss/sdk instead of being defined per-surface.
export { isRequestUserInputAction } from "@semoss/sdk";
