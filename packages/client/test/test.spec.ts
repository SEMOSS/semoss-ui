// Simple test to validate that vitest is working
import { describe, it, expect } from 'vitest';

describe('vitest setup check 2', () => {
    it('should run a basic test', () => {
        expect(1 + 1).toBe(2);
    });
});
