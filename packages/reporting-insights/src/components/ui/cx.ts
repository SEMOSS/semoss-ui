/**
 * Class name joiner — backed by the shared @semoss/ui `cn` (clsx + tailwind-merge)
 * so this app resolves Tailwind class conflicts the same way as the rest of
 * SemossWeb. Kept exported as `cx` for call-site compatibility.
 */
export { cn as cx } from "@semoss/ui/next";
